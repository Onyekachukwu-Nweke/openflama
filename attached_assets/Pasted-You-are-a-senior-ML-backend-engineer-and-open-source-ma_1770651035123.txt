You are a senior ML + backend engineer and open-source maintainer working inside the existing GitHub repo "openflama".

TASK: Create the first production-quality financial agent focused on Value Investing, with a pluggable LLM backbone (Ollama / llama.cpp / Qwen / others). The agent must be understandable and extendable by contributors. You must also document the full creation workflow in a dedicated docs file, and provide explicit instructions for committing and pushing changes to GitHub.

========================
HIGH-LEVEL GOALS
========================
1) Implement a "Value Investing Agent" that can:
   - produce a structured company analysis (business, moat, management, financial health, valuation, risk)
   - compute/estimate key value metrics (P/E, P/B, EV/EBITDA, FCF yield, ROIC proxies, margin trends, leverage)
   - generate a conservative intrinsic value range using simple methods (DCF-lite, multiples, margin-of-safety logic)
   - output a decision framework (buy/hold/watch/avoid) with assumptions and uncertainty
   - be compatible with later integration into Agentfunds.io via the TypeScript SDK.

2) Provide a pluggable LLM provider layer:
   - Must support at least:
     (a) Ollama local provider (HTTP to localhost)
     (b) A "llama.cpp" provider stub (document how to wire)
     (c) A generic "OpenAI-like" provider interface stub (no keys required, placeholder only)
   - The agent must allow selecting provider by config/env:
     FLAMA_LLM_PROVIDER=ollama|llamacpp|openai_stub
     FLAMA_OLLAMA_MODEL=qwen2.5:7b (example), or llama3, mistral, etc.

3) Document the full workflow so other contributors can create new agents.
   - Add a doc: docs/agents/creating-agents.md
   - Add agent README: agents/value-investing-agent/README.md
   - Include configuration examples, prompts, tool interfaces, testing instructions.

4) Provide instructions for git commit + push at the end.

========================
IMPLEMENTATION DETAILS
========================

A) Location / Structure
Create a new agent package at:
  /agents/value-investing-agent/

Include:
  /agents/value-investing-agent/agent.py (or main module)
  /agents/value-investing-agent/prompts/ (system + templates)
  /agents/value-investing-agent/config.example.yaml
  /agents/value-investing-agent/tests/ (basic tests)
  /agents/value-investing-agent/README.md

Also add shared interfaces in a suitable place (choose ONE and be consistent):
  Option 1: /packages/core-py/flama/llm_providers/*
  Option 2: /agents/_shared/llm_providers/*
Prefer Option 1 if the repo already has /packages/core-py as the canonical python library.

B) Agent I/O Contract
Implement a single entrypoint function (and CLI if possible):
  run_value_investing_agent(
      ticker: str,
      as_of_date: str | None,
      provider: str,
      model: str,
      options: dict
  ) -> dict

Return JSON with a stable schema:
{
  "ticker": "...",
  "as_of_date": "...",
  "thesis": {...},
  "financials_summary": {...},
  "valuation": {...},
  "moat_management": {...},
  "risks": [...],
  "recommendation": {"action": "buy|hold|watch|avoid", "margin_of_safety": "...", "confidence": 0-1},
  "assumptions": [...],
  "sources": [...],
  "trace": {
      "llm_provider": "...",
      "llm_model": "...",
      "tools_called": [...],
      "timings": {...}
  }
}

C) Data / Tools
For MVP, do NOT depend on paid APIs.
Implement at least one of the following:
- A simple yfinance-based data fetcher (prices + basic financial statements if available).
- If the repo already has data ingestion code, reuse it.

Tool design:
- Create a "tool" interface so the LLM can request:
  - get_company_profile(ticker)
  - get_financial_statements(ticker)
  - get_price_history(ticker, window)
  - compute_value_metrics(financials, price)
  - intrinsic_value_estimate(metrics, assumptions)

If full tool-calling is not implemented yet, do deterministic orchestration:
- Step 1: Fetch data
- Step 2: Compute metrics
- Step 3: Ask LLM to write the narrative + justify assumptions using a prompt template.

D) LLM Provider Layer
Implement a base interface:
class LLMProvider:
  def generate(self, system_prompt: str, user_prompt: str, temperature: float=0.2) -> str

Implement:
1) OllamaProvider:
   - Calls Ollama local endpoint (default http://localhost:11434)
   - Uses model from env FLAMA_OLLAMA_MODEL
   - Must fail gracefully with helpful error if Ollama not running.

2) LlamaCppProvider (stub acceptable for MVP):
   - Provide a placeholder class and docs showing how to connect (HTTP server or python binding).
   - Must raise NotImplementedError with instructions.

3) OpenAIStubProvider:
   - No external calls. Return a deterministic placeholder response to keep tests passing if users don't have LLM setup.
   - This is for CI and local dev without LLM.

E) Configuration
- Provide config.example.yaml supporting:
  provider, model, temperature, horizon, safety_margin, etc.
- Provide env-based overrides.

F) Documentation
Create docs files:
1) docs/agents/creating-agents.md
   Must include:
   - folder conventions
   - agent I/O schema
   - how to add prompts and tools
   - how to add LLM provider support
   - how to run tests
   - how to publish changes / open PR

2) agents/value-investing-agent/README.md
   Must include:
   - what it does
   - quickstart (with and without Ollama)
   - example commands
   - example output JSON snippet
   - how to switch between qwen/llama/mistral in Ollama
   - troubleshooting

G) Tests
Add minimal tests to validate:
- schema keys exist
- provider selection works
- OpenAIStubProvider path works (so CI passes)
If yfinance introduces flakiness, mock it in tests.

H) Optional CLI
If easy, add:
  python -m agents.value_investing_agent --ticker AAPL --provider ollama --model qwen2.5:7b

========================
DELIVERABLES
========================
- A working agent in /agents/value-investing-agent
- Pluggable LLM providers implemented as described
- Documentation files created
- Tests created and runnable
- At the end, print:
  1) exact commands to run the agent locally with Ollama
  2) exact commands to run with stub provider
  3) exact git commands to commit and push (do NOT include any token in output)

========================
PUSH INSTRUCTIONS (REQUIRED OUTPUT)
========================
At the end of your work, output the following (as plain text):
- git status
- suggested commit message
- commands:
  git add .
  git commit -m "Add Value Investing Agent with pluggable LLM providers"
  git push origin main

IMPORTANT SECURITY RULES:
- Never print secrets or environment variable values.
- Never write tokens into any file.
- If remote is already configured, do not modify it.

Now implement the above in the existing openflama repo. Start by inspecting the current repository structure and reusing existing conventions (packages, lint/test tooling, folder naming). Then add the agent, docs, and tests.
